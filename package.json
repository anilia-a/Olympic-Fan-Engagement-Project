{
  "name": "olympicsfanengagement",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel ./*.html",
    "build": "rm -rf .parcel-cache && parcel build ./*.html --dist-dir ./build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}
